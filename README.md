These dotfiles are designed for use on **Arch Linux** with **Qtile** as the window manager.
This repository will be updated regularly as Arch Qtile is my daily driver.

## Key Features

- **Qtile** configuration, optimized with **qtile-extras** for a visually appealing bar.
- **Zsh** with **zimfw**, enhanced with custom scripts and plugins (`fzf`, `zoxide`, `tldr`).
- **Neovim** setup using **Lazy Vim** for plugin management.
- **Kitty** as the default terminal and scratchpads in Qtile.
- **Tmux** with **TPM** (Tmux Plugin Manager) for effective session management.
- **Yazi** is the terminal-based file manager.
- **Sayonara** music player configured with a custom CSS for dark mode.
- **Fonts**: inter-font for the display and ttf-jetbrains-mono-nerd for the terminal.
- **Arch-specific** tweaks and optimizations, tested on **Arch with Qtile** using the **LTS Kernel**.

## Installation

### Clone the Repository and use GNU Stow to create symlinks.

```bash
git clone https://github.com/moturri/dot.git ~/.dot
